<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  <p><% if(user_signed_in?) %><%= current_user.email %><%end%></p>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Gyms</h1>
    <%= link_to 'New gym', new_gym_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <%# <div id="gyms" class="min-w-full">
    <%= render @gyms %>
  <%# </div> %>
</div>
